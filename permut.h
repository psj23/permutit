#if defined(MSDOS) || defined(_MSDOS) || defined(__DOS__) || defined(__MSDOS__)
#define MAXN (size_t) 8 
#else
#define MAXN (size_t) 12
#endif

#define FAILUR EXIT_FAILURE
#define SUCCES EXIT_SUCCESS

#define BUFLEN 82

